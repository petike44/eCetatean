# Supabase — project dashboard → Settings → API
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Backend API — pick ONE approach for local dev:
# A) Leave empty and use Vite proxy (vite.config.ts proxies /api → :3001) — recommended
# B) Set explicit URL: VITE_API_URL=http://localhost:3001
VITE_API_URL=
